#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/common.sh"

require_docker_services
require_rgb_tools
ensure_dirs

manual_step "rgb-transfer-taker" \
  "Use the taker RGB invoice as the recipient, create a maker transfer, copy the consignment to taker state, mine one block, and accept the transfer in the taker stash."
